COnvo with ilya:

Need to store persistent database via shared DB 

need backend service via rest API to facilitate the DB 

Explore lambda service, or 


-- Store Maps API key onto github as a secret and then have the app reference --> 


add billing reference number (for any payment type entered, if blank do not show on pdf)

On work order, Add abillity to enter Info: Unit #, Reference # 

Auto-save all user changes on work orders/bills (do not make them have to hit 'save changes' each time)


---------------------------------------



Template line items should be same table as Parts & Labor catalog (User can select parts that have price to be template line item and have specific qty)
Think like SF data model

Account/Contact rating system ?
-give 1/5 stars --> if they have 1 star it warns you when creating new work order 

reminder to backup every 2/6 hours


Make 'tap to call' available on WO screen

display work type in the WO list (On each record, if available if not do not display

Allow user to choose icon for work type in admin setting when adding/editing a work type (have an icon library to select from)

Date filters on work order list (Salesforce-listview style)
Search work orders by bill #



"Google cloud sync" as a home-page global action
Last-sync timestamp + stale-sync warning (>30 min)
Field help text explaining where data lands on the PDF
--Sample PDF to see sample
---Under Admin setting - have option to show 'Master PDF preview' to see which fields go where




Need to build a file system of sorts for this app/build askill that help sme capture key detail about this project like:

1. Where to deploy too and how (beta or stable, version control,etc)
2. Notes on how to get up to speed quick on this project and its proceses (deployments etc)
3. idea list to atuo exdcture on --> build improvements doc on its own/idea tracker/roadmap





